#include <stdio.h>

#define QTD_ELEMENTOS 5

int main() {
    
}

void digitaVetor(v[], n) {
    for (int i = 0 ; i < n ; i += 1){
        printf("Digite um valor: ");
        scanf("%d", v[i]);
    }
}

void definirMax(v[], n) {
    maior = v[0];
    for (int i = 0 ; i < n ; i += 1){
        if (maior < v[i]) {
            maior = v[i];
        }
    }
}

void definirOrdem (v[], n) {
    digitaVetor(v[] , n);
    while 
} 