#include <stdio.h>

int main() {
    
    for(int i = 1 ; i = 50 ; i += 1) {
        if ( i % 3 == 0 || i % 10 == 3) {
        printf(i);
        printf(", ");
        } else {
            printf("PI")
        }
    }
    return 0;
}